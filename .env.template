# .env.template — Copy to .env.local and fill in values
# DO NOT commit .env.local to Git

PP_TENANT_ID=
PP_APP_ID=
PP_CLIENT_SECRET=
PP_ENV_DEV=https://carremacodeapps.crm.dynamics.com

# Connectivity pill — non-secret, used for traceability in the UI
VITE_DV_CONNECTION_ID=
VITE_DV_ENV_ID=

# Feature flag: show the Adjust Criticality control (write to SN criticality assessment).
# Until systemuser/role tables are registered for a proper role gate, this flag is the
# hard gate. Leave unset (hidden) in Prod; set to "true" for Dev stewards.
VITE_ENABLE_CRITICALITY_EDIT=
